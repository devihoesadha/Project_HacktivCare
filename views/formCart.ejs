<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <title>Cart</title>
</head>

<body>
  <center>
    <h1>Cart List</h1>
    <% if (dataCart.length  < 1) { %>
      <h1>Your Cart is Empty</h1>
      <% } else{%>
    <div class="container-fluid gap-5 d-flex flex-wrap mt-4">
      <% dataCart.forEach(el=> { %>
              <div class="mb-3">
                <div class="card" style="width: 15rem;">
                  <img class="card-img-top" src="<%= el.Product.imageURL %>" alt="error picture" style=" max-height: 400px;">
                  <div class="card-body">
                    <h4 class="card-title">
                      <%= el.Product.name %>
                    </h4>
                    <p class="card-text">
                      <%= formatRupiah(el.Product.price)%>
                    </p>
                    <a href="/cart/delete/<%= el.Product.id %>"><button>delete</button></a>
                  </div>
                </div>
              </div>
              <% }) %>
            </div>
            <% } %>
            <h2>Total Price = <%= formatRupiah(sumPrice) %></h2>
          <a href="/products"><button>product liSt</button></a>
  </center>
</body>

</html>